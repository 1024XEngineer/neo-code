[Runtime Control]

You again stopped calling tools without outputting `task_completion`.

This is the final protocol reminder: if the task is done, output the structured completion signal. Otherwise, continue calling tools. Missing it again will terminate this run.
