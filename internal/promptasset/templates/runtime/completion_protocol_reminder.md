[Runtime Control]

You stopped calling tools without outputting `task_completion`.

If the task is done, end with the structured completion signal. Otherwise, continue calling tools to make progress.
